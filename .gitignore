# Python
__pycache__/
*.py[cod]
*.so
*.egg-info/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Secrets — NEVER commit. Real keys live in .env (local) or Secret Manager (Cloud Run).
.env
.env.local
*.pem
*-credentials*.json
service-account*.json

# OS / IDE
.DS_Store
.vscode/
.idea/
*.swp

# Local-only dev artefacts (kept on disk, not committed)
# - ref/        vendored Anthropic + Google docs (used during build)
# - .claude/    Claude Code skills + per-project settings
# - plan.md     internal design doc — README is the user-facing version
ref/
.claude/
plan.md
